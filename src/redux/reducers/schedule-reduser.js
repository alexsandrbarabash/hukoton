const defaultState = {
  numberWeek: 0,
  date: new Date(),
  data: null,
};

const scheduleReduser = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};