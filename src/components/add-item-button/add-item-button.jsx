import React from 'react';

import './add-item-button.scss';

const AddItemButton = (mode) => {
  return (
    <button className="btn btn-primary add-item-button">+</button>
  )
}

export default AddItemButton;